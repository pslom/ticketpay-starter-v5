import React from "react";

type Props = {
  className?: string;
  size?: "sm" | "md" | "lg";
};

export default function Wordmark(_props: Props) {
  return null; // no visual output (removes TP mark)
}